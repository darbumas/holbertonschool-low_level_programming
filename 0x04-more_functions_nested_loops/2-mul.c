#include "holberton.h"

/**
 * mul - ƒ() multiplies two integers
 * @a: input integer
 * @b: input integer
 * Return: the product
 */
int mul(int a, int b)
{

	int product = (a * b);

	return (product);
}
