#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0
 */

int main(void)
{
    char quote[] = {"with proper grammar, but the outcome is a piece of art,"};

    printf("%s\n", quote);
    return (0);
}
